### Running regression and KMeans

To build the files the files need to be build seperately. To run the regression file use the following command`make bin/regression`

To run the kmeans file run the command `make bin/kmeans`