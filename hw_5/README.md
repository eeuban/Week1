### Running regression and KMeans

To build the files the files need to be build seperately. To build the regression program use the following command `make bin/regression`. To run use the command `./bin/regression`.

To build the kmeans program run the command `make bin/kmeans`. To run use the command `./bin/kmeans`.